import XCTest
@testable import Movve_Network_Framework

final class Movve_Network_FrameworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Movve_Network_Framework().text, "Hello, World!")
    }
}
